# Supabase

Migrations and local config for the ALBA inquiry CRM.

```bash
# From repo root (requires Supabase CLI + linked project)
supabase db push
# or apply SQL in the Dashboard SQL editor
```

See `migrations/` for the schema defined in [docs/prd.md](../docs/prd.md) §7.
